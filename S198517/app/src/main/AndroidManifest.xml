<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.simenarvnes.s198517" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpActivity" android:parentActivityName=".MainActivity"></activity>
        <activity android:name=".SignInActivity" android:parentActivityName=".MainActivity"></activity>
        <activity android:theme="@style/Theme" android:name=".ProfileActivity" android:parentActivityName=".MainActivity"></activity>
        <activity android:theme="@style/ThemeLight" android:name=".SearchActivity" android:parentActivityName=".ProfileActivity"></activity>
        <activity android:theme="@style/Theme" android:name=".InvitationActivity" android:parentActivityName=".ProfileActivity"></activity>
        <activity android:theme="@style/Theme" android:name=".RequestActivity" android:parentActivityName=".ProfileActivity"></activity>
    </application>

</manifest>
